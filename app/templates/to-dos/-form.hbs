<form {{action 'save' model on="submit"}}>
  <p>{{errorMessage}}</p>
  <p>
    <label>To Do Description</label>
    {{input value=model.description}}
  </p>
  <button type="submit" value="Add todo" class="button">Add todo</button>
  <button {{action "cancel"}}>Cancel</button>
</form>