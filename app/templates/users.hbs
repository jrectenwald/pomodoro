<h3>Users:</h3>
<ul>
  {{#each model as |user|}}
    <li>
      {{#link-to 'users.show' user}}
        {{user.firstName}} {{user.lastName}}
      {{/link-to}}
    </li>
  {{/each}}
</ul>

{{link-to 'new user' 'users.new'}}

{{outlet}}